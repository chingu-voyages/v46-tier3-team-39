/*
Custom Resolvers Needed:
- Generate a single question, using another question as a template + category + tag context (use OpenAI API) (Irha)
- Adds, deletes, and updates a question's data into db ! (Fung)
- Adds, deletes, and updates a set questions to a quiz (Fung)
- Adds, deletes, and updates a quiz (Fung)
- Create Question Routes (Irha)
- Create User Info Routes (Irha)
*/